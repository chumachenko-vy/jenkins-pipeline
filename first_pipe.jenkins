#!groovy
// test
properties([disableCocurrentBuilds()])

pipeline {
    agent{
        label 'master'
        }
    options {
        buildDiDiscarder(logRotator(numToKeepsStr: '10', artifactNumToKeepStr: '10')
        timestamps()
    }
    stages {
        stage("Preparations") {
            steps {
                sh 'ssh ssh ubuntu@35.153.226.18 \'hostname\''
            }
         }
     }
 }
